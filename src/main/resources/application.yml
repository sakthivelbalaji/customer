server:
    port: 8090
spring:
    datasource:
        password: Welcome123
        url: jdbc:postgresql://localhost:5432/customer
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
    mvc:
        view:
            prefix: /
            suffix: .jsp
